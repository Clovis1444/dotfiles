[user]
	name = Clovis1444
	email = eu11111444@gmail.com
[pull]
	rebase = true
[credential]
	helper = store

; delta pager setup
[core]
    pager = delta

[interactive]
    diffFilter = delta --color-only

[delta]
    navigate = true  # use n and N to move between diff sections
    dark = true      # or light = true, or omit for auto-detection
    side-by-side = true
    line-numbers = true

[merge]
    conflictstyle = zdiff3
